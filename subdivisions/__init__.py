"""Burr Ridge subdivision tagging — self-contained initiative.

Tags warehouse transactions by the named/signed subdivision they sit in. Read-only over the
warehouse; all outputs land under this tree (data/, docs/). See SUBDIVISION_TAGGING_PLAN.md.
Run modules from the repo root: `python -m subdivisions.<module>`.
"""
